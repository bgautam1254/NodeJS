extract the content and navigate to main directory of the code, open terminal and type
1. "npm i"
2. "node index.js"

// ------------------------------------------------------------------------------------------------
1. signup
// ------------------------------------------------------------------------------------------------
POST --> http://localhost:3000/api/auth/signup

{ 
    "username" : "yourUsername",
    "password" : yourPassword"
}

// ------------------------------------------------------------------------------------------------
2. login 
// ------------------------------------------------------------------------------------------------
POST --> http://localhost:3000/api/auth/login

{ 
    "username" : "yourCreatedUsername",
    "password" : yourCreatedPassword"
}

// ------------------------------------------------------------------------------------------------
3. POST --> tickets --> http://localhost:3000/api/tickets
// ------------------------------------------------------------------------------------------------

{
    "token": "generatedToken",
    "numberOfTickets": numberOfTicketsToGenerate-6
}

// ------------------------------------------------------------------------------------------------
4. GET --> tickets --> http://localhost:3000/api/tickets
// ------------------------------------------------------------------------------------------------

{
    "token": "generatedToken",
    "ticketId": "generatedTicketId"
}

// ------------------------------------------------------------------------------------------------
5. GET --> tickets pagination --> http://localhost:3000/api/tickets?page=1&size=3
// ------------------------------------------------------------------------------------------------

{
    "token": "generatedToken",
    "ticketId": "generatedTicketId"
}

// ------------------------------------------------------------------------------------------------